{
  "name": "@mosaic-code/test-data-factory-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "generate-og": "node scripts/generate-og.mjs",
    "bundle-data": "node scripts/bundle-data.mjs",
    "test:playwright": "playwright test"
  },
  "dependencies": {
    "@mosaic-code/test-data-factory": "file:..",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "sharp": "^0.32.6",
    "smartcrop-sharp": "^2.0.6",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  },
  "//bundle-data-note": "bundle-data and generate-og need sibling repos checked out locally; install ../../test-data-* via npm install <path> --no-save if regenerating data/og."
}
